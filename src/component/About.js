import React from 'react'
// import noteContext from '../context/notes/noteContext'

const About = ()=>  {
    // const a = useContext(noteContext)
    // useEffect(() => {
    //     a.update();
    // }, []) context
    return (
        <div>
            
            {/* This is about {a.state.name} and is in {a.state.class} FOR CONText purpose */}
        </div>
    )
}

export default About;
